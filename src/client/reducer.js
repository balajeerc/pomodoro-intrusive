/*
 * Reducer managing state changes for pomodoro-intrusive's client process
 */

function reducer(store = {}, action) {
  if (!action) {
    return store;
  }
  return store;
}

export default reducer;
